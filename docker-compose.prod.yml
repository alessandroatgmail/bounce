name: "bounce_2_0"

services:

  # ── Frontend + Nginx reverse proxy ───────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      DOMAIN_NAME: ${DOMAIN_NAME}
      NGINX_ENVSUBST_FILTER: "DOMAIN_NAME"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - static_volume:/django_static:ro
      - media_volume:/django_media:ro
      - certbot_certs:/etc/letsencrypt:ro
      - certbot_challenge:/var/www/certbot:ro
    depends_on:
      - web

  # ── Certbot ───────────────────────────────────────────────────────
  # Requests and renews Let's Encrypt certificates.
  # Runs once on startup, then checks renewal every 12 hours.
  certbot:
    image: certbot/certbot:latest
    volumes:
      - certbot_certs:/etc/letsencrypt
      - certbot_challenge:/var/www/certbot
    # Renew certificates every 12 hours if they are close to expiry.
    # Let's Encrypt certs expire after 90 days; renewal happens at 60 days.
    entrypoint: >
      sh -c "trap exit TERM;
             while :; do
               certbot renew --webroot --webroot-path=/var/www/certbot --quiet;
               sleep 12h & wait $${!};
             done"

volumes:
  certbot_certs:
  certbot_challenge: