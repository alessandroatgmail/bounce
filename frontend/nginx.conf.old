# Redirect all HTTP traffic to HTTPS
server {
    listen 80;
    server_name _;

    # Let's Encrypt challenge — certbot needs this to verify domain ownership
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect everything else to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

# Main HTTPS server
; server {
;     listen 443 ssl;
;     server_name review-toba.bounceswinglovers.com;
;
;     # SSL certificates managed by certbot
;     ssl_certificate /etc/letsencrypt/live/review-toba.bounceswinglovers.com/fullchain.pem;
;     ssl_certificate_key /etc/letsencrypt/live/review-toba.bounceswinglovers.com/privkey.pem;
;
;     # Modern SSL settings
;     ssl_protocols TLSv1.2 TLSv1.3;
;     ssl_caches shared:SSL:10m;
;     ssl_session_timeout 10m;
;
;     # ── React app ─────────────────────────────────────────────────
;     location / {
;         root /usr/share/nginx/html;
;         index index.html;
;         try_files $uri $uri/ /index.html;
;     }
;
;     # ── Django API ────────────────────────────────────────────────
;     location /api/ {
;         proxy_pass http://web:8000;
;         proxy_set_header Host $host;
;         proxy_set_header X-Real-IP $remote_addr;
;         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
;         proxy_set_header X-Forwarded-Proto $scheme;
;     }
;
;     # ── Django Admin ──────────────────────────────────────────────
;     location /admin/ {
;         proxy_pass http://web:8000;
;         proxy_set_header Host $host;
;         proxy_set_header X-Real-IP $remote_addr;
;         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
;         proxy_set_header X-Forwarded-Proto $scheme;
;     }
;
;     # ── Django static files ───────────────────────────────────────
;     location /static/ {
;         alias /django_static/;
;     }
;
;     # ── Django media files ────────────────────────────────────────
;     location /media/ {
;         alias /django_media/;
;     }
;
;     # ── WebSocket ─────────────────────────────────────────────────
;     location /ws/ {
;         proxy_pass http://web:8000;
;         proxy_http_version 1.1;
;         proxy_set_header Upgrade $http_upgrade;
;         proxy_set_header Connection "upgrade";
;         proxy_set_header Host $host;
;     }
; }